<div class="middle-box text-center loginscreen  animated fadeInDown">
  <div>
    <div>

      <%= link_to landing_index_path do %>
          <h1 class="logo-name">i 21</h1>
      <% end %>

    </div>
    <h3><b>Acceso Corporativo</b></h3>
    <p>
      Contacta al administrador del portal en caso de no contar con tus credenciales.
    </p>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group">
          <%= f.email_field :email, autofocus: true, placeholder: 'Email', class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.password_field :password, autocomplete: "off", placeholder: 'Contraseña', class: 'form-control' %>
        </div>
        <% if devise_mapping.rememberable? -%>
            <div class="field">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
        <% end -%>
        <%= f.submit 'Login', class: 'btn btn-primary block full-width m-b' %>
        <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
            <%= link_to 'Register', new_registration_path(resource_name), class: 'btn btn-secondary block full-width m-b' %><br />
        <% end -%>
    <% end %>
  </div>
</div>